{% extends "HOBlogBundle::layout.html.twig" %}

{% block hoblog_body %}

<div class="page-header">
	<h1>Delete Article</h1>
</div>

<div class="well">
	<p>
		Are you sure you want to delete the article "{{ article.title }}" ?
	</p>

	<form action="{{ path('ho_blog_delete', {'id': article.id}) }}" method='POST'>
	<a href="{{ path('ho_blog_view', {'id': article.id }) }}" class="btn btn-default"><i class="glyphicon glyphicon-chevron-left"></i>Retour à l'article</a>
	<input type="submit" value="Delete" class="btn btn-danger" />
	{{ form_rest(form) }}
	</form>
</div>

{% endblock %}